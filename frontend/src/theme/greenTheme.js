import { createTheme } from '@mui/material';

const greenTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#019031'
        },
        legendary: {
            main: '#019031'
        }
    }
});

export default greenTheme;